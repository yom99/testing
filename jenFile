pipeline {
    agent any

    stages {
        stage('initial') {
            steps {
               git 'https://github.com/yom99/testing.git'
            }
        }
         stage('run') {
            steps {
               sh 'npm install'
                sh 'npm start'
            }
        }
        
        
      }
}
