pipeline {
    agent any

    stages {
        stage('initial') {
            steps {
               git 'https://github.com/yom99/testing.git'
            }
        }
        stage('start server') {
            steps {
               call 'npm install'
            }
        }
    }
}
